import{_ as s,o as t,c as a,a0 as i}from"./chunks/framework.B3sz4m_N.js";const k=JSON.parse('{"title":"Conventions and structure","description":"Theme layout, classic vs block posture, autoloading, templates, and coding standards.","frontmatter":{"title":"Conventions and structure","description":"Theme layout, classic vs block posture, autoloading, templates, and coding standards."},"headers":[],"relativePath":"reports/theme-only-audit/01-conventions-and-structure.md","filePath":"reports/theme-only-audit/01-conventions-and-structure.md"}'),o={name:"reports/theme-only-audit/01-conventions-and-structure.md"};function n(d,e,l,r,c,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_01-—-conventions-structure" tabindex="-1">01 — Conventions &amp; Structure <a class="header-anchor" href="#_01-—-conventions-structure" aria-label="Permalink to &quot;01 — Conventions &amp; Structure&quot;">​</a></h1><p>This document describes the theme&#39;s current shape: how it&#39;s organised, which conventions it follows, and where those conventions diverge from WordPress guidance. It contains no performance, security, or dependency findings — those live in <a href="./reports/theme-only-audit/03-performance.html">03</a>, <a href="./reports/theme-only-audit/04-security.html">04</a>, and <a href="./reports/theme-only-audit/05-dependencies.html">05</a>.</p><p>All file references are relative to <code>wordpress/wp-content/themes/theme-only/</code> unless noted.</p><h2 id="contents" tabindex="-1">Contents <a class="header-anchor" href="#contents" aria-label="Permalink to &quot;Contents&quot;">​</a></h2><ul><li><a href="#theme-classification">Theme classification</a></li><li><a href="#directory-layout">Directory layout</a></li><li><a href="#autoloading">Autoloading</a></li><li><a href="#stylecss-and-themejson-contracts">style.css and theme.json contracts</a></li><li><a href="#template-hierarchy">Template hierarchy</a></li><li><a href="#naming-and-identity">Naming and identity</a></li><li><a href="#coding-standards-php">Coding standards (PHP)</a></li><li><a href="#coding-standards-cssscss-js">Coding standards (CSS/SCSS, JS)</a></li><li><a href="#internationalisation-i18n">Internationalisation (i18n)</a></li><li><a href="#findings">Findings</a></li></ul><h2 id="theme-classification" tabindex="-1">Theme classification <a class="header-anchor" href="#theme-classification" aria-label="Permalink to &quot;Theme classification&quot;">​</a></h2><p>This is a <strong>hybrid classic theme</strong> — neither a pure classic theme nor a true block theme:</p><table tabindex="0"><thead><tr><th>Signal</th><th>Observation</th><th>Implication</th></tr></thead><tbody><tr><td><code>index.php</code>, <code>page.php</code>, <code>single.php</code>, <code>header.php</code>, <code>footer.php</code>, <code>404.php</code>, <code>search.php</code>, <code>searchform.php</code></td><td>Present; drive rendering via <code>get_header()</code> / <code>the_content()</code> / <code>get_footer()</code></td><td>Classic theme template hierarchy</td></tr><tr><td><code>theme.json</code> (version 2)</td><td>Minimal — declares only palette and layout widths</td><td>Block editor reads palette, front-end does not (see below)</td></tr><tr><td><code>Setup::removeGlobalStyles()</code> (<code>inc/Setup.php:320-325</code>)</td><td>Removes <code>wp_enqueue_global_styles</code> on <code>init</code> priority 100</td><td><code>theme.json</code> styling is actively suppressed on the front end; only the admin editor sees it</td></tr><tr><td><code>patterns/</code> + <code>Patterns::registerTemplates/Patterns/LayoutPatterns</code> (<code>inc/Patterns.php</code>)</td><td><code>register_block_pattern</code> used for homepage/blog templates, cards/CTA blocks, 100/50-50/33-33-33 layout patterns</td><td>Some block-theme scaffolding, but no FSE templates (<code>templates/*.html</code>)</td></tr><tr><td><code>templates/default.php</code> (<code>wordpress/wp-content/themes/theme-only/templates/default.php</code>)</td><td>Single PHP page template, not an HTML block template</td><td>Confirms classic template model</td></tr><tr><td>Post body rendering</td><td>Authored almost exclusively through 27 <code>acf/*</code> ACF-Pro blocks (<code>inc/Blocks.php</code>)</td><td>Content is block-serialised in <code>post_content</code>, rendered server-side through <code>acf_rendered_block()</code></td></tr><tr><td><code>allowedBlockTypes</code> filter (<code>inc/Blocks.php:102-157</code>)</td><td>Returns an explicit allow-list containing 25 <code>acf/*</code> blocks + <code>core/block</code> only</td><td>Core blocks are disabled everywhere except the reusable-block wrapper</td></tr><tr><td><code>register_block_pattern</code> with <code>blockTypes: [&#39;core/post-content&#39;]</code> + <code>postTypes: [&#39;page&#39;/&#39;post&#39;]</code></td><td>Starter patterns for homepage/blog-post templates</td><td>Editors get pattern-based starters when creating new posts</td></tr></tbody></table><p><strong>Implication for the audit:</strong> the theme is positioned somewhere on the spectrum &quot;classic theme with a bolted-on block editor&quot; → &quot;block theme&quot;. It does not commit to either model cleanly. This hybrid posture is a root cause for several findings in other documents — most notably that <code>theme.json</code> tokens are authored but never applied to the front end (<a href="./reports/theme-only-audit/03-performance.html">03 — Performance</a>), and that the <code>patterns/</code> scaffolding is undermined by the <code>allowedBlockTypes</code> allow-list (<a href="./reports/theme-only-audit/06-blocks-and-components.html">06 — Blocks and components</a>).</p><h2 id="directory-layout" tabindex="-1">Directory layout <a class="header-anchor" href="#directory-layout" aria-label="Permalink to &quot;Directory layout&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wordpress/wp-content/themes/theme-only/</span></span>
<span class="line"><span>├── 404.php, searchform.php, single.php, page.php, index.php, search.php</span></span>
<span class="line"><span>├── header.php, footer.php</span></span>
<span class="line"><span>├── style.css                            # Theme stylesheet header only</span></span>
<span class="line"><span>├── theme.json                           # Minimal v2 global settings</span></span>
<span class="line"><span>├── screenshot.png</span></span>
<span class="line"><span>├── composer.json, composer.lock         # Theme-level Composer</span></span>
<span class="line"><span>├── package.json, pnpm-lock.yaml         # Theme-level npm (pnpm)</span></span>
<span class="line"><span>├── pnpm-workspace.yaml                  # pnpm workspace config</span></span>
<span class="line"><span>├── webpack.config.js, webpackAlias.js   # Custom Webpack 5 build</span></span>
<span class="line"><span>├── phpcs.xml                            # PSR-12 ruleset</span></span>
<span class="line"><span>├── .eslintrc.js, .stylelintrc.js</span></span>
<span class="line"><span>├── assets/                              # Source assets</span></span>
<span class="line"><span>│   ├── fonts/  icons/  img/  js/  scss/</span></span>
<span class="line"><span>├── inc/                                 # PHP — EDWP\\ namespace</span></span>
<span class="line"><span>│   ├── ACF.php  Admin.php  Ajax.php  Blocks.php  Filters.php</span></span>
<span class="line"><span>│   ├── GravityForms.php  Helpers.php  Media.php  Modals.php</span></span>
<span class="line"><span>│   ├── Patterns.php  PostTypes.php  RankMath.php  Render.php</span></span>
<span class="line"><span>│   ├── Setup.php  Svg.php  TinyMCE.php  WPML.php</span></span>
<span class="line"><span>│   ├── ACF/           # Post.php, ThemeOptions.php (ACF field definitions)</span></span>
<span class="line"><span>│   ├── Blocks/        # 27 block folders (one dir per block)</span></span>
<span class="line"><span>│   ├── Components/    # 11 component folders (same pattern as blocks)</span></span>
<span class="line"><span>│   ├── Globals/       # FieldGroups.php, Options.php (shared ACF helpers)</span></span>
<span class="line"><span>│   ├── PostTypes/     # CaseStudy, GlobalBlock, Person, Testimonial</span></span>
<span class="line"><span>│   └── Taxonomies/    # RelatedService</span></span>
<span class="line"><span>├── partials/</span></span>
<span class="line"><span>│   └── global/        # favicons, header, footer, mega-menu, mobile-menu,</span></span>
<span class="line"><span>│                      #   modal, password-form</span></span>
<span class="line"><span>├── patterns/</span></span>
<span class="line"><span>│   ├── blocks/        # block patterns</span></span>
<span class="line"><span>│   ├── layouts/       # layout patterns</span></span>
<span class="line"><span>│   └── templates/     # starter templates</span></span>
<span class="line"><span>└── templates/</span></span>
<span class="line"><span>    └── default.php    # Sole classic page template</span></span></code></pre></div><p>Each <code>inc/Blocks/{Name}/</code> and <code>inc/Components/{Name}/</code> folder has the same internal shape:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>inc/Blocks/Cards/</span></span>
<span class="line"><span>├── Cards.php          # Class with identical shell across all 27 blocks</span></span>
<span class="line"><span>├── block.json         # WP block metadata (category, editorScript, style, acf.blockVersion)</span></span>
<span class="line"><span>├── template.php       # Rendered HTML template (extract($args) supplied by Render::partial)</span></span>
<span class="line"><span>├── styles.scss        # Per-block SCSS (webpack discovers it)</span></span>
<span class="line"><span>└── scripts.js         # Per-block JS (optional; not present for every block)</span></span></code></pre></div><h3 id="observations" tabindex="-1">Observations <a class="header-anchor" href="#observations" aria-label="Permalink to &quot;Observations&quot;">​</a></h3><ul><li>The per-block folder convention is a clean boundary — it maps one block to one folder, with code, template, and styles co-located. This is the single strongest structural decision in the codebase.</li><li>Every block + component PHP file is a <strong>copy-paste of the same 80-line shell</strong> (see <code>initialiseBlock()</code>, <code>filterBlockFields()</code>, <code>registerBlock()</code>, <code>renderBlock()</code>, <code>setupBlockACF()</code>). This is a <code>P1 Effort:M</code> finding covered in <a href="./reports/theme-only-audit/06-blocks-and-components.html">06</a> and <a href="./reports/theme-only-audit/07-reusability-and-plugin-extraction.html">07</a> — the whole shell belongs in a base class.</li><li><code>inc/Globals/</code> is a sensible name but conflates <strong>two concerns</strong>: ACF field composition (<code>FieldGroups.php</code>) and value enumerations / choice lists (<code>Options.php</code>). These are different abstractions: one is ACF-specific, the other is domain-data. They should not share a namespace.</li><li><code>partials/</code> vs <code>templates/</code> vs <code>patterns/</code> vs <code>inc/Blocks/*/template.php</code> — the theme has four different places templates can live. The distinction is: <code>partials/global/</code> = classic-theme template parts rendered via <code>Render::partial()</code>; <code>templates/</code> = classic WP <code>Template Name:</code> templates; <code>patterns/*.html</code> = block-editor pattern files registered by <code>Patterns.php</code>; <code>inc/Blocks/*/template.php</code> = per-block server-render templates called by <code>Render::partial()</code> from inside the block&#39;s <code>render_callback</code>. The overlap is manageable but worth documenting in the theme README.</li></ul><h2 id="autoloading" tabindex="-1">Autoloading <a class="header-anchor" href="#autoloading" aria-label="Permalink to &quot;Autoloading&quot;">​</a></h2><p>The theme declares a Composer PSR-4 autoloader (<code>composer.json:57-61</code>):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;autoload&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;psr-4&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;EDWP\\\\&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inc/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>…and then <strong>also registers a hand-rolled SPL autoloader</strong> in <code>functions.php:11-18</code>:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">spl_autoload_register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ($classname) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    $class   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> str_replace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\\\</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DIRECTORY_SEPARATOR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str_replace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;_&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;-&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $classname));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    $classes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dirname</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">__FILE__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DIRECTORY_SEPARATOR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;inc&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DIRECTORY_SEPARATOR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $class </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;.php&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">file_exists</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">($classes)) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        require_once</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $classes;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>This second autoloader is:</p><ol><li><strong>Redundant</strong> with Composer&#39;s PSR-4 autoloader that&#39;s already loaded on the line above (<code>require_once __DIR__ . &#39;/vendor/autoload.php&#39;;</code>).</li><li><strong>Wrong</strong> — the <code>str_replace(&#39;_&#39;, &#39;-&#39;)</code> transform replaces underscores with hyphens in class names, which does not match PSR-4 or the on-disk layout (no class in the codebase has an underscore in its name; no file on disk has a hyphen). The autoloader only fires for classes Composer hasn&#39;t already resolved, and because all classes in <code>EDWP\\</code> are PSR-4 under <code>inc/</code>, it never actually loads anything. It is dead code sitting at the top of <code>functions.php</code>.</li></ol><p><strong>Finding <code>ARCH-01</code> (P2, S).</strong> Delete <code>spl_autoload_register</code> block from <code>functions.php</code>. Rely on Composer&#39;s PSR-4.</p><h3 id="functions-php-side-effects" tabindex="-1"><code>functions.php</code> side effects <a class="header-anchor" href="#functions-php-side-effects" aria-label="Permalink to &quot;\`functions.php\` side effects&quot;">​</a></h3><p><code>functions.php:36-52</code> instantiates every subsystem at file-load time:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ACF</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Admin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Ajax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> RankMath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">is_plugin_active</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sitepress-multilingual-cms/sitepress.php&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> WPML</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is the theme&#39;s <strong>service-locator</strong>. Each class registers its own hooks in its constructor. Implications:</p><ol><li>Every class constructor runs on every request, whether or not the class&#39;s hooks will fire. Admin classes load on the front end and vice versa. This is a minor perf cost (~hundreds of microseconds per request); see <a href="./reports/theme-only-audit/03-performance.html">03</a>.</li><li>There is <strong>no dependency injection</strong>. A class cannot be replaced at construction time. If a downstream project wanted a modified <code>Modals</code> system, the only intervention points are the hooks the class itself exposes — which, for most subsystems, is zero. This is the core problem <a href="./reports/theme-only-audit/07-reusability-and-plugin-extraction.html">07 — Reusability and plugin extraction</a> addresses.</li><li><code>is_plugin_active()</code> is called here at file-load time. That function lives in <code>wp-admin/includes/plugin.php</code> and <strong>is not loaded on front-end requests by default</strong>. This is a latent fatal error if someone&#39;s front-end fails to include the file (e.g. from a WP-CLI command, a REST-only request, or any context where admin includes haven&#39;t been bootstrapped). Covered in detail in <a href="./reports/theme-only-audit/02-non-native-apis.html">02</a> and <a href="./reports/theme-only-audit/04-security.html">04</a>.</li></ol><p><strong>Finding <code>ARCH-02</code> (P1, S).</strong> Replace the service-locator in <code>functions.php</code> with a single bootstrap class hooked to <code>after_setup_theme</code>. Each subsystem should declare the hook priority at which it wants to register. This gives future extraction a single seam.</p><p><strong>Finding <code>ARCH-03</code> (P1, S).</strong> Replace <code>is_plugin_active()</code> with <code>defined(&#39;ICL_SITEPRESS_VERSION&#39;)</code> or <code>class_exists(&#39;\\SitePress&#39;)</code>. Same fix applies to <code>inc/Blocks/NewsletterSignup/NewsletterSignup.php:112</code> which calls it at class-registration time inside <code>blockFields()</code>.</p><h2 id="style-css-and-theme-json-contracts" tabindex="-1"><code>style.css</code> and <code>theme.json</code> contracts <a class="header-anchor" href="#style-css-and-theme-json-contracts" aria-label="Permalink to &quot;\`style.css\` and \`theme.json\` contracts&quot;">​</a></h2><h3 id="style-css" tabindex="-1"><code>style.css</code> <a class="header-anchor" href="#style-css" aria-label="Permalink to &quot;\`style.css\`&quot;">​</a></h3><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/*!</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Theme Name: EDWP Theme</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Author: Elixirr</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Author URI: http://elixirr.com/</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Version: 1.0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Text domain: edwp</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span></code></pre></div><p>This is missing multiple fields the <a href="https://developer.wordpress.org/themes/getting-started/main-stylesheet-style-css/" target="_blank" rel="noreferrer">WordPress Theme Review Handbook</a> requires for any theme intended for distribution:</p><ul><li><code>Description:</code> — required</li><li><code>License:</code> — required (most commonly <code>GPLv2 or later</code>)</li><li><code>License URI:</code> — required</li><li><code>Tested up to:</code> — required (recommended WP version)</li><li><code>Requires at least:</code> — required (minimum WP version)</li><li><code>Requires PHP:</code> — required (minimum PHP version)</li><li><code>Tags:</code> — required for theme directory; optional for private distribution</li><li><code>Template:</code> — only needed for child themes</li></ul><p>These are not strictly mandatory for a theme deployed only on internal/client sites (the theme never goes through the wordpress.org review process), but they are used by WordPress core at runtime: <code>Tested up to:</code> + <code>Requires at least:</code> + <code>Requires PHP:</code> drive the &quot;this theme may not be compatible with your WordPress version&quot; notices in Admin → Themes. Their absence means those notices never fire, and an incompatible install fails silently.</p><p><strong>Finding <code>STYLE-01</code> (P2, S).</strong> Add the missing <code>style.css</code> headers. Choose the real values: <code>Requires at least: 6.5</code>, <code>Requires PHP: 8.1</code>, <code>License: GPL-2.0-or-later</code>, <code>License URI: https://www.gnu.org/licenses/gpl-2.0.html</code>, etc.</p><p>The comment is also opened with <code>/*!</code> (exclamation-mark variant used by minifiers to preserve a comment). WordPress reads <code>style.css</code> as plain text and doesn&#39;t require the bang. Harmless, but unusual in a theme header.</p><h3 id="theme-json" tabindex="-1"><code>theme.json</code> <a class="header-anchor" href="#theme-json" aria-label="Permalink to &quot;\`theme.json\`&quot;">​</a></h3><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;$schema&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://schemas.wp.org/trunk/theme.json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;settings&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;color&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;palette&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">…</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;"> colours…</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ] },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;layout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;contentSize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1374px&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;wideSize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1728px&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Three issues:</p><ol><li><strong><code>$schema</code> points to <code>trunk</code></strong>, not a pinned WP version. <code>trunk</code> is the unstable-development schema. On a production theme this should point to a specific WP version&#39;s schema, e.g. <code>https://schemas.wp.org/wp/6.5/theme.json</code>, or (since WP 6.6) <code>https://schemas.wp.org/trunk/theme.json</code> is acceptable if you explicitly want &quot;whatever is current&quot; — but pin it unless you have a reason not to.</li><li><strong><code>version: 2</code></strong> — current latest is version 3 (since WP 6.6). Moving to 3 unlocks enhancements like <code>settings.blocks.defaults</code> and improvements to <code>styles.variations</code> scoping. Not a bug, but a staleness signal.</li><li><strong>The file is semantically dead on the front end</strong> because <code>Setup::removeGlobalStyles()</code> strips the core <code>wp_enqueue_global_styles</code> hook at <code>init:100</code> (<code>inc/Setup.php:320-325</code>). The palette and layout declared here only affect the <strong>block editor</strong>. No class applies those colour tokens to front-end CSS. This is a significant conceptual breakage: editors pick a brand colour from the block palette, and it never manifests on the site. Covered under <a href="./reports/theme-only-audit/03-performance.html">03</a> as <code>PERF-THEME-JSON</code> and cross-referenced in <a href="./reports/theme-only-audit/06-blocks-and-components.html">06</a>.</li></ol><p><strong>Finding <code>THEMEJSON-01</code> (P1, S).</strong> Decide: either commit to <code>theme.json</code> (stop suppressing <code>wp_enqueue_global_styles</code>, extend the file to carry real tokens, migrate block SCSS to the token system) or delete the file. The current middle ground gives the user the worst of both worlds — editor palette that doesn&#39;t apply visually.</p><p><strong>Finding <code>THEMEJSON-02</code> (P2, S).</strong> Pin <code>$schema</code> to a WP version; migrate to <code>version: 3</code>.</p><h2 id="template-hierarchy" tabindex="-1">Template hierarchy <a class="header-anchor" href="#template-hierarchy" aria-label="Permalink to &quot;Template hierarchy&quot;">​</a></h2><p>The theme provides the classic PHP template hierarchy but sparsely:</p><table tabindex="0"><thead><tr><th>Template</th><th>Content</th></tr></thead><tbody><tr><td><code>index.php</code></td><td><code>get_header(); while(have_posts()) the_post(); the_content(); endwhile; get_footer();</code></td></tr><tr><td><code>page.php</code></td><td>Same as index, plus <code>post_password_required()</code> handling</td></tr><tr><td><code>single.php</code></td><td>Same as page, but <strong>does not call <code>get_header()</code> / <code>get_footer()</code></strong> — bug; see below</td></tr><tr><td><code>404.php</code></td><td><code>get_header(); echo &#39;404&#39;; get_footer();</code> — placeholder content</td></tr><tr><td><code>search.php</code></td><td><code>get_header();</code> → <code>get_search_form();</code> → loop → <code>get_footer();</code></td></tr><tr><td><code>searchform.php</code></td><td>Hand-rolled <code>&lt;form role=&quot;search&quot;&gt;</code> with <code>schema.org/WebSite</code> microdata</td></tr><tr><td><code>templates/default.php</code></td><td>Named classic template (<code>Template Name: Default Template</code>) — placeholder</td></tr></tbody></table><p><strong>Finding <code>TPL-01</code> (P0, S).</strong> <code>single.php</code> omits <code>get_header()</code> and <code>get_footer()</code> calls:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// single.php — current</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">post_password_required</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    \\EDWP\\Render</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">partial</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;partials/global/password-form&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    the_content</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>There is no <code>get_header()</code> or <code>get_footer()</code> call anywhere in this file. Single posts render without a header or footer. This is either a bug or a very unusual intentional choice. Confirm before fixing; if unintended, wrap the existing code with <code>get_header();</code> … <code>get_footer();</code>.</p><p><strong>Finding <code>TPL-02</code> (P1, S).</strong> <code>404.php</code> renders the literal string <code>&#39;404&#39;</code> — a placeholder. This must be replaced before the theme goes to production. Content-side 404 pages are also possible (WP supports <code>404</code> pattern / block templates).</p><p><strong>Finding <code>TPL-03</code> (P2, S).</strong> <code>search.php</code> outputs <code>&lt;h1&gt;&lt;a href=&quot;…&quot;&gt;{title}&lt;/a&gt;</code> for each result — the closing <code>&lt;/h1&gt;</code> is missing. Minor.</p><p><strong>Finding <code>TPL-04</code> (P2, S).</strong> <code>templates/default.php</code> is a named classic template that renders nothing except its title. If it&#39;s not used, delete it; if it&#39;s a scaffold, move it to <code>patterns/templates/</code> as a block pattern or remove it.</p><h3 id="template-part-resolution" tabindex="-1">Template-part resolution <a class="header-anchor" href="#template-part-resolution" aria-label="Permalink to &quot;Template-part resolution&quot;">​</a></h3><p><code>header.php</code> and <code>footer.php</code> use <code>EDWP\\Render::partial(&#39;partials/global/…&#39;)</code> for loading template parts rather than core&#39;s <code>get_template_part()</code> / <code>get_header(&#39;name&#39;)</code>. <code>Render::partial()</code> is a custom template-loader with added behaviour (object caching, <code>extract($args)</code>, argument passing). The function carries multiple correctness bugs, covered in <a href="./reports/theme-only-audit/02-non-native-apis.html#renderpartial-bugs">02</a> and <a href="./reports/theme-only-audit/04-security.html">04</a>.</p><p><strong>Finding <code>TPL-05</code> (P1, S).</strong> Prefer <code>get_template_part($slug, $name, $args)</code> (native, 5.5+) over <code>Render::partial()</code>. Native <code>get_template_part</code> supports a <code>$args</code> array since 5.5, passes them to the template (accessible as <code>$args</code>), fires <code>get_template_part</code> and <code>get_template_part_{$slug}</code> actions, and honours the template hierarchy (<code>locate_template()</code>). This is also a precondition for theme-part overrides by plugins.</p><h2 id="naming-and-identity" tabindex="-1">Naming and identity <a class="header-anchor" href="#naming-and-identity" aria-label="Permalink to &quot;Naming and identity&quot;">​</a></h2><p>The theme has <strong>four distinct names</strong> across different files:</p><table tabindex="0"><thead><tr><th>File</th><th>Name present</th></tr></thead><tbody><tr><td><code>style.css</code></td><td><code>EDWP Theme</code></td></tr><tr><td><code>composer.json</code> (root + theme-level)</td><td><code>edx/edwp</code></td></tr><tr><td><code>package.json</code> (theme-level)</td><td><code>&quot;project-name&quot;: &quot;mourant&quot;</code></td></tr><tr><td><code>webpack.config.js</code></td><td>Defaults to <code>&#39;mourant&#39;</code> / <code>&#39;theme-only&#39;</code> via <code>process.env.PROJECT_NAME</code></td></tr><tr><td>Namespace + text domain</td><td><code>EDWP\\\\</code>, <code>&#39;edwp&#39;</code></td></tr><tr><td>Directory name</td><td><code>theme-only</code></td></tr><tr><td>Top-level README</td><td>References &quot;EDX WordPress MAMP/Docker Starter Template&quot;</td></tr></tbody></table><p>This looks like the residue of one starter template being cloned for a client (&quot;Mourant&quot;) then partially rebranded. It is disorienting on its own merits and makes search/replace during project setup error-prone. The <code>setup/create.sh</code> script does not appear to fix this.</p><p><strong>Finding <code>ID-01</code> (P2, S).</strong> Pick one identity (likely <code>edwp</code> for the agency starter, <code>{client_slug}</code> for per-project clones) and apply it consistently across <code>style.css</code>, <code>composer.json</code>, <code>package.json</code>, webpack defaults, namespace, text domain, and directory name. Document the rename steps in the README&#39;s &quot;install&quot; section and automate in <code>setup/create.sh</code> if this is genuinely a starter template.</p><h2 id="coding-standards-php" tabindex="-1">Coding standards (PHP) <a class="header-anchor" href="#coding-standards-php" aria-label="Permalink to &quot;Coding standards (PHP)&quot;">​</a></h2><p><code>phpcs.xml</code> declares PSR-12 as the base ruleset:</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">rule</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PSR12&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">exclude</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Generic.Files.LineLength&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">rule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>…and <code>composer.json:18-24</code> runs PHPCS with <code>--runtime-set testVersion 7.4</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lint:php&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;phpcs --runtime-set testVersion 7.4 … --standard=phpcs.xml ./*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>The codebase, however, uses PHP-8-only syntax in many places:</p><table tabindex="0"><thead><tr><th>Syntax</th><th>Example</th><th>Minimum PHP</th></tr></thead><tbody><tr><td>Union types</td><td><code>array|string $image</code> in <code>Filters::fixWpGetAttachmentImageSvg</code></td><td>8.0</td></tr><tr><td><code>mixed</code> type</td><td><code>mixed $value</code> in <code>ACF::filterFieldValues</code></td><td>8.0</td></tr><tr><td><code>str_contains()</code></td><td><code>Helpers::processVimeoURL</code></td><td>8.0</td></tr><tr><td>Named arguments</td><td><code>Render::partial(…, echo: false)</code> everywhere</td><td>8.0</td></tr><tr><td>Array spread into arrays</td><td><code>[...$args, &#39;classes&#39; =&gt; …]</code> — every block <code>filterBlockFields</code></td><td>8.1 (the string-key spread form)</td></tr><tr><td>Constructor <code>int|string</code> param in <code>ACF::filterFieldValues</code></td><td>8.0</td><td></td></tr></tbody></table><p>The <code>phpcs</code> config&#39;s <code>testVersion 7.4</code> will accept this code because it lints syntactically — <code>7.4-</code> would reject it; <code>7.4</code> just sets the target floor without forbidding later features. But the signal is confusing: the lint says &quot;we support 7.4&quot; while the code requires 8.1.</p><p><strong>Finding <code>PHP-01</code> (P1, S).</strong> Update <code>phpcs.xml</code> and the <code>lint:php</code> script to <code>--runtime-set testVersion 8.1-</code> to match reality. Add <code>Requires PHP: 8.1</code> to <code>style.css</code> (see <code>STYLE-01</code>). Update <code>composer.json</code> to <code>&quot;require&quot;: { &quot;php&quot;: &quot;&gt;=8.1&quot; }</code>.</p><h3 id="lint-ruleset-choice" tabindex="-1">Lint ruleset choice <a class="header-anchor" href="#lint-ruleset-choice" aria-label="Permalink to &quot;Lint ruleset choice&quot;">​</a></h3><p>PSR-12 is a respectable choice but is <strong>not the same as WordPress Coding Standards</strong>. WPCS (<code>WordPress</code>, <code>WordPress-Core</code>, <code>WordPress-VIP-Go</code>) ships rules specific to WordPress: nonces, capability checks, output escaping, sanitisation, prefixed global functions/variables, hook naming conventions. By using PSR-12 the theme is opting out of all of them.</p><p>Given that the stated direction is <strong>&quot;standardise as much with WordPress working practices&quot;</strong> (see <a href="./reports/theme-only-audit/00-summary.html">00 — Summary</a> and <a href="./reports/theme-only-audit/02-non-native-apis.html">02 — Non-native APIs</a>), PSR-12 is the wrong ruleset.</p><p><strong>Finding <code>PHP-02</code> (P1, M).</strong> Adopt <a href="https://github.com/WordPress/WordPress-Coding-Standards" target="_blank" rel="noreferrer"><code>WordPress-Extra</code></a> rules (which includes <code>WordPress-Core</code> + best-practice rules like escaping/sanitisation checks) as the primary ruleset. Optionally layer PHPCS PHPCompatibility alongside. Keep PSR-12-specific rules (brace placement, visibility) if the team prefers — they can co-exist with WPCS. Expect a non-trivial noise-to-signal ratio on the first pass. Create a baseline to make adoption incremental (covered in <a href="./reports/theme-only-audit/08-roadmap.html">08 — Roadmap</a>).</p><p><strong>Finding <code>PHP-03</code> (P2, S).</strong> Add <a href="https://phpstan.org/" target="_blank" rel="noreferrer">PHPStan</a> with the WordPress extension (<code>szepeviktor/phpstan-wordpress</code>) at level 5+. The existing code&#39;s type-safety isn&#39;t bad (most methods are typed), so level 5 should be achievable. PHPStan will catch the <code>call_user_method()</code> dead-code issue in <code>Render::partial()</code> (<a href="./reports/theme-only-audit/02-non-native-apis.html">02</a>) and the unused field reference in <code>CaseStudy::setupFields()</code>. See <code>skills/wp-phpstan</code> in the agent-skills bundle for configuration.</p><h2 id="coding-standards-css-scss-js" tabindex="-1">Coding standards (CSS/SCSS, JS) <a class="header-anchor" href="#coding-standards-css-scss-js" aria-label="Permalink to &quot;Coding standards (CSS/SCSS, JS)&quot;">​</a></h2><h3 id="scss" tabindex="-1">SCSS <a class="header-anchor" href="#scss" aria-label="Permalink to &quot;SCSS&quot;">​</a></h3><ul><li><code>stylelint-config-standard-scss</code>, <code>stylelint-config-standard</code>, <code>stylelint-prettier</code>, <code>stylelint-scss</code>, <code>stylelint-order</code>, <code>stylelint-selector-bem-pattern</code>, <code>stylelint-webpack-plugin</code>.</li><li>This is a strong, modern stack. No findings here — it&#39;s above the water-line for agency CSS.</li></ul><h3 id="js" tabindex="-1">JS <a class="header-anchor" href="#js" aria-label="Permalink to &quot;JS&quot;">​</a></h3><ul><li><code>.eslintrc.js</code> is 543 bytes; likely a very thin config.</li><li>No ESLint rule for React/Gutenberg (no <code>@wordpress/eslint-plugin</code>). If and when any native block dev work happens, <code>@wordpress/eslint-plugin</code> is the right ruleset.</li><li>Code uses ESM imports + Babel <code>@babel/preset-env</code> with <code>modules: false</code> for tree-shaking. Clean setup.</li></ul><p><strong>Finding <code>JS-01</code> (P2, S).</strong> When beginning native block migration (<a href="./reports/theme-only-audit/08-roadmap.html">08 — Roadmap</a>), add <code>@wordpress/eslint-plugin</code> for React/Gutenberg rules. Until then, no change needed.</p><h2 id="internationalisation-i18n" tabindex="-1">Internationalisation (i18n) <a class="header-anchor" href="#internationalisation-i18n" aria-label="Permalink to &quot;Internationalisation (i18n)&quot;">​</a></h2><h3 id="translations-load" tabindex="-1">Translations load <a class="header-anchor" href="#translations-load" aria-label="Permalink to &quot;Translations load&quot;">​</a></h3><p><code>Setup::afterThemeSetup()</code> calls:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">load_theme_textdomain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;edwp&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get_template_directory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;/languages&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>This has been optional since WP 4.6 when a theme is installed under <code>wp-content/themes/</code>. It&#39;s still valid and harmless — WP automatically loads <code>languages/*.mo</code> files for themes in the WP install root, but an explicit call is a safe belt-and-braces.</p><p>The <code>wp-content/languages/</code> directory has 72 files — there are real translations in the repo for multiple locales. Good.</p><h3 id="text-domain-usage" tabindex="-1">Text-domain usage <a class="header-anchor" href="#text-domain-usage" aria-label="Permalink to &quot;Text-domain usage&quot;">​</a></h3><p>Text domain <code>&#39;edwp&#39;</code> is used consistently. Admin.php, Setup.php, most blocks, and most helpers wrap user-facing strings in <code>__()</code> / <code>_x()</code> / <code>esc_html__()</code> / <code>esc_html_x()</code>. This is good discipline.</p><p><strong>Finding <code>I18N-01</code> (P2, S).</strong> A handful of user-facing strings escape i18n: <code>&#39;&amp;hellip;&#39;</code> return value from <code>Setup::setExcerptMore()</code> (acceptable — it&#39;s an HTML entity), <code>&#39;404&#39;</code> in <code>404.php</code> (acceptable only because it&#39;s a placeholder; see <code>TPL-02</code>), the modal content field hard-coded labels in <code>ACF/ThemeOptions.php</code> (<code>&#39;Modal Content&#39;</code>, <code>&#39;Modal Settings&#39;</code>, <code>&#39;Modal ID&#39;</code>, <code>&#39;Modal Title&#39;</code>, <code>&#39;e.g., &quot;contact&quot;&#39;</code>). These are admin-side strings — they should still be translatable for agencies working with non-English-speaking admins.</p><h3 id="timezone-date-formatting" tabindex="-1">Timezone/date formatting <a class="header-anchor" href="#timezone-date-formatting" aria-label="Permalink to &quot;Timezone/date formatting&quot;">​</a></h3><p><code>Helpers::getPostInfo()</code> uses <code>get_the_date(&#39;d F Y&#39;, …)</code> — hard-coded British format. <code>partials/global/footer.php:122</code> uses <code>date(&#39;Y&#39;)</code> directly — correct for year-only, but the canonical WP idiom is <code>wp_date(&#39;Y&#39;)</code> (respects site timezone) or <code>current_time(&#39;Y&#39;)</code>.</p><p><strong>Finding <code>I18N-02</code> (P2, S).</strong> Replace <code>date()</code> calls with <code>wp_date()</code> / <code>current_time()</code> in templates. Replace hard-coded date formats with the site&#39;s <code>get_option(&#39;date_format&#39;)</code> where appropriate, or expose as a filter.</p><h2 id="findings-summary" tabindex="-1">Findings summary <a class="header-anchor" href="#findings-summary" aria-label="Permalink to &quot;Findings summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Title</th><th>Severity</th><th>Effort</th></tr></thead><tbody><tr><td>ARCH-01</td><td>Delete redundant <code>spl_autoload_register</code> in <code>functions.php</code></td><td>P2</td><td>S</td></tr><tr><td>ARCH-02</td><td>Replace service-locator with a bootstrap class hooked to <code>after_setup_theme</code></td><td>P1</td><td>S</td></tr><tr><td>ARCH-03</td><td>Replace <code>is_plugin_active()</code> calls with <code>class_exists</code>/<code>defined</code></td><td>P1</td><td>S</td></tr><tr><td>STYLE-01</td><td>Add required <code>style.css</code> headers (<code>Requires PHP</code>, <code>License</code>, etc.)</td><td>P2</td><td>S</td></tr><tr><td>THEMEJSON-01</td><td>Commit to or remove <code>theme.json</code> (currently suppressed on front end)</td><td>P1</td><td>S</td></tr><tr><td>THEMEJSON-02</td><td>Pin <code>$schema</code>; migrate to <code>theme.json</code> v3</td><td>P2</td><td>S</td></tr><tr><td>TPL-01</td><td><code>single.php</code> omits <code>get_header()</code> / <code>get_footer()</code></td><td><strong>P0</strong></td><td>S</td></tr><tr><td>TPL-02</td><td><code>404.php</code> renders placeholder literal <code>&#39;404&#39;</code></td><td>P1</td><td>S</td></tr><tr><td>TPL-03</td><td>Missing <code>&lt;/h1&gt;</code> in <code>search.php</code></td><td>P2</td><td>S</td></tr><tr><td>TPL-04</td><td>Review or remove <code>templates/default.php</code></td><td>P2</td><td>S</td></tr><tr><td>TPL-05</td><td>Replace <code>Render::partial</code> with native <code>get_template_part($slug, $name, $args)</code></td><td>P1</td><td>M</td></tr><tr><td>ID-01</td><td>Unify theme identity across <code>style.css</code>, <code>composer.json</code>, <code>package.json</code>, webpack, namespace</td><td>P2</td><td>S</td></tr><tr><td>PHP-01</td><td>Update <code>phpcs</code> target to PHP 8.1; fix <code>composer.json</code> PHP constraint</td><td>P1</td><td>S</td></tr><tr><td>PHP-02</td><td>Adopt <code>WordPress-Extra</code> PHPCS ruleset</td><td>P1</td><td>M</td></tr><tr><td>PHP-03</td><td>Add PHPStan with WordPress extension</td><td>P2</td><td>S</td></tr><tr><td>JS-01</td><td>Add <code>@wordpress/eslint-plugin</code> during native block migration</td><td>P2</td><td>S</td></tr><tr><td>I18N-01</td><td>Wrap remaining admin strings in <code>__()</code></td><td>P2</td><td>S</td></tr><tr><td>I18N-02</td><td>Replace <code>date()</code> with <code>wp_date()</code> / <code>current_time()</code> in templates</td><td>P2</td><td>S</td></tr></tbody></table><p>Next: <a href="./reports/theme-only-audit/02-non-native-apis.html">02 — Non-native APIs and native replacements</a></p>`,96)])])}const g=s(o,[["render",n]]);export{k as __pageData,g as default};
